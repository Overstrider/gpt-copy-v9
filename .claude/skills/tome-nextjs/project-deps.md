# project-deps.md

Detected deps. Scaffold — agent fills on first run from `package.json`.

## Next.js
- next: ?.?
- react: ?.?
- router: App | Pages
- typescript: ?.?

## Data fetching
- @tanstack/react-query: ? | swr: ?

## UI / styling
- tailwindcss: ?.? | css modules | styled-components
- shadcn/ui | radix-ui | ? (design system)

## Forms / validation
- react-hook-form: ?.?
- zod: ?.? | yup: ?

## Testing
- vitest | jest
- @testing-library/react
- playwright: ? (e2e — usually top-level)

## State
- zustand | redux-toolkit | jotai | context-only

## Notes
- Re-scan on `package.json` change.
- Fill `?` before proceeding.
